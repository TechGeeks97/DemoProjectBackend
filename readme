# Project Title

A brief and catchy description of your project.

## Installation

1. **Clone the repository:**

    ```bash
    git clone https://github.com/your-username/your-project.git
    ```

2. **Navigate to the project directory:**

    ```bash
    cd your-project
    ```

3. **Install dependencies:**

    ```bash
    yarn
    ```

## Configuration

1. **Create a configuration file (.env) in the root of your project:**

    ```env
    PORT=3000
    MONGO_URI=mongodb://localhost:27017/your-database-name
   
    # Add any other necessary configuration variables
    ```

    Modify the values according to your setup.

## Development

To run the project in development mode, use the following command:

```bash
npm run dev


## Production

To run the project in development mode, use the following command:

```bash
npm run start

## For Api Documentation

 Check Dahsboard_API.html reveal it in browser